/** Tailwind CSS 4 — ayri bir tailwind.config.js YOKTUR.
 *  Tum tasarim token'lari src/app/(frontend)/globals.css icindeki @theme blogundadir. */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
